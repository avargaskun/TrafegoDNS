name: pr-check

on:
  pull_request:
    branches: [dev]

permissions:
  contents: read

jobs:
  pr-check:
    name: pr-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '23.11.1'
      - run: npm ci
      - name: npm audit
        run: |
          echo '### npm audit --omit=dev --audit-level=high' >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          set +e
          npm audit --omit=dev --audit-level=high | tee -a "$GITHUB_STEP_SUMMARY"
          rc=${PIPESTATUS[0]}
          set -e
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          exit "$rc"
      - name: Test
        run: |
          if [ -d test ]; then npm test; else echo "No test/ directory yet; skipping npm test"; fi
      - uses: docker/setup-buildx-action@v3
      - name: Build image (amd64, no push)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./docker-s6/Dockerfile
          platforms: linux/amd64
          push: false
